{
  "name": "@openbody/openbody-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenBody reference implementation (TypeScript): validate + §8.3 canonical normalize + conformance vector runner. Apache-2.0.",
  "license": "Apache-2.0",
  "scripts": {
    "vectors": "tsx scripts/run-vectors.ts",
    "lossless": "tsx scripts/test-lossless.ts",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run lossless && npm run vectors"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "canonicalize": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
